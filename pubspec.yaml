name: states_rebuilder_groqhealth
description: Modified version of states_rebuilder 4.0.0+1 for Groq Health.
version: 4.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.14.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0
  flutter_localizations:
    sdk: flutter 

flutter: